# react-particle-effect-button@1.0.1 声明 peer react ^15||^16，与项目 React 19 冲突。
# 该库仅用于展示面板的粒子胶囊实现，已用 postinstall 补丁修复其生命周期问题，
# 与 React 19 实际可兼容，故忽略 peer 冲突（npm ci 与 npm install 均生效）。
legacy-peer-deps=true