name: Build Client Apps

on:
  push:
    branches: [ main, native-client ]
  workflow_dispatch:
    inputs:
      server_url:
        description: "服务器地址（push 触发时使用 Secrets 中的 NEXT_PUBLIC_SERVER_URL）"
        required: false
        type: string
        default: "http://192.168.1.2:3000"

concurrency:
  group: build-client-${{ github.ref }}
  cancel-in-progress: false

# ==================== 构建触发逻辑 ====================
# commit 信息中包含关键字决定构建哪个平台：
#   exe -> Windows EXE
#   apk -> Android APK
#   ipa -> iOS IPA
# 例如 "exe ipa" 会同时构建 exe 和 ipa。
#
# 服务器地址优先级：
#   1. workflow_dispatch 时手动输入的 server_url
#   2. GitHub Secrets 中的 NEXT_PUBLIC_SERVER_URL
#   3. 默认值 http://localhost:3000（本地测试用）
# 请在 GitHub Settings → Secrets and variables → Actions 中配置 NEXT_PUBLIC_SERVER_URL

env:
  SERVER_URL: ${{ inputs.server_url || secrets.NEXT_PUBLIC_SERVER_URL || 'http://192.168.1.2:3000' }}

jobs:
  # ==================== Windows EXE ====================
  build-exe:
    if: ${{ github.event_name == 'workflow_dispatch' || (github.event_name == 'push' && contains(github.event.head_commit.message, 'exe')) }}
    runs-on: windows-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: npm

      - name: Set up Rust
        uses: dtolnay/rust-toolchain@stable

      - name: Install Tauri CLI (Rust)
        run: cargo install tauri-cli --locked

      - name: Install npm dependencies
        run: npm ci --fetch-retries=5 --fetch-retry-mintimeout=10000 --fetch-retry-maxtimeout=60000

      - name: Build frontend
        env:
          NEXT_PUBLIC_SERVER_URL: ${{ env.SERVER_URL }}
        run: npm run build:tauri

      - name: Build Windows EXE
        run: cargo tauri build --no-bundle

      - name: Upload EXE
        uses: actions/upload-artifact@v4
        with:
          name: bili-live-exe
          path: src-tauri/target/release/*.exe

  # ==================== Android APK ====================
  build-apk:
    if: ${{ github.event_name == 'workflow_dispatch' || (github.event_name == 'push' && contains(github.event.head_commit.message, 'apk')) }}
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: npm

      - name: Set up JDK 17
        uses: actions/setup-java@v4
        with:
          java-version: "17"
          distribution: "temurin"

      - name: Set up Android SDK
        uses: android-actions/setup-android@v3
        with:
          packages: "platforms;android-34 build-tools;34.0.0 ndk;27.0.12077973"

      - name: Set up Rust
        uses: dtolnay/rust-toolchain@stable
        with:
          targets: aarch64-linux-android,armv7-linux-androideabi,i686-linux-android,x86_64-linux-android

      - name: Install Tauri CLI (Rust)
        run: cargo install tauri-cli --locked

      - name: Install Linux dependencies
        run: |
          sudo apt-get update
          sudo apt-get install -y libwebkit2gtk-4.1-dev libappindicator3-dev librsvg2-dev patchelf libssl-dev

      - name: Install npm dependencies
        run: npm ci --fetch-retries=5 --fetch-retry-mintimeout=10000 --fetch-retry-maxtimeout=60000

      - name: Build frontend
        env:
          NEXT_PUBLIC_SERVER_URL: ${{ env.SERVER_URL }}
        run: npm run build:tauri

      - name: Init Android project
        run: cargo tauri android init

      - name: Generate app icons for Android
        run: cargo tauri icon

      - name: Build Android APK
        env:
          ANDROID_HOME: ${{ env.ANDROID_HOME }}
          NDK_HOME: ${{ env.ANDROID_HOME }}/ndk/27.0.12077973
          JAVA_HOME: ${{ env.JAVA_HOME }}
        run: cargo tauri android build --target aarch64

      - name: Sign APK with debug keystore
        env:
          ANDROID_HOME: ${{ env.ANDROID_HOME }}
        run: |
          if [ ! -f "$HOME/.android/debug.keystore" ]; then
            mkdir -p "$HOME/.android"
            keytool -genkeypair -v -keystore "$HOME/.android/debug.keystore" \
              -storepass android -alias androiddebugkey -keypass android \
              -keyalg RSA -keysize 2048 -validity 10000 \
              -dname "CN=Android Debug,O=Android,C=US"
          fi
          APK=$(find src-tauri/gen/android/app/build/outputs/apk -name "*.apk" | head -n 1)
          echo "Signing APK: $APK"
          "$ANDROID_HOME/build-tools/34.0.0/zipalign" -f 4 "$APK" "$APK.aligned"
          "$ANDROID_HOME/build-tools/34.0.0/apksigner" sign \
            --ks "$HOME/.android/debug.keystore" \
            --ks-pass pass:android --key-pass pass:android \
            --ks-key-alias androiddebugkey \
            --out "$APK" "$APK.aligned"
          rm -f "$APK.aligned"
          echo "Signed APK ready: $APK"

      - name: Upload APK
        uses: actions/upload-artifact@v4
        with:
          name: bili-live-android
          path: |
            src-tauri/gen/android/app/build/outputs/apk/universal/release/*.apk
            src-tauri/gen/android/app/build/outputs/apk/release/*.apk

  # ==================== iOS IPA ====================
  build-ipa:
    if: ${{ github.event_name == 'workflow_dispatch' || (github.event_name == 'push' && contains(github.event.head_commit.message, 'ipa')) }}
    runs-on: macos-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: npm

      - name: Set up Rust
        uses: dtolnay/rust-toolchain@stable
        with:
          targets: aarch64-apple-ios,x86_64-apple-ios,aarch64-apple-ios-sim

      - name: Install Tauri CLI (Rust)
        run: cargo install tauri-cli --locked

      - name: Install npm dependencies
        run: npm ci --fetch-retries=5 --fetch-retry-mintimeout=10000 --fetch-retry-maxtimeout=60000

      - name: Build frontend
        env:
          NEXT_PUBLIC_SERVER_URL: ${{ env.SERVER_URL }}
        run: npm run build:tauri

      - name: Init iOS project
        run: cargo tauri ios init

      - name: Generate app icons for iOS
        run: cargo tauri icon

      - name: Build iOS IPA
        run: cargo tauri ios build --target aarch64 --no-sign

      - name: Upload IPA
        uses: actions/upload-artifact@v4
        with:
          name: bili-live-ios
          path: |
            src-tauri/gen/apple/build/*/*.ipa
            src-tauri/gen/ios/build/*/*.ipa