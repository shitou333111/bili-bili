# See https://help.github.com/articles/ignoring-files/ for more about ignoring files.

# dependencies
/node_modules
/.pnp
.pnp.*
.yarn/*
!.yarn/patches
!.yarn/plugins
!.yarn/releases
!.yarn/versions

# testing
/coverage

# next.js
/.next/
/out/

# tauri rust build artifacts
/src-tauri/target/

# data - exclude all runtime data (user records, sessions, snapshots, backups)
.data/

# production
/build

# misc
.DS_Store
*.pem

# debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.pnpm-debug.log*

# env files (can opt-in for committing if needed)
.env*

# vercel
.vercel

# tauri default icon (replaced by public/orig_icon.png)
app-icon.png

# minisign keys (hot-update signing) — never commit private keys
*.key
*.key.b64
hot-update.key
hot-update.pub
minisign.key
minisign.pub

# test scratch files
.fttest/

# 个人辅助脚本（舰长入场动画抓取），不进 git
/scripts/fetch-guard-effects.mjs

# notes
TO DO.txt

# typescript
*.tsbuildinfo
next-env.d.ts
